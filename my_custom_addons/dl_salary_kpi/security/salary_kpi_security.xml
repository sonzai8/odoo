<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Record Rule for dl.salary.kpi.month -->
        <record id="dl_salary_kpi_month_multi_company_rule" model="ir.rule">
            <field name="name">Bảng cân đối lương: đa công ty</field>
            <field name="model_id" ref="model_dl_salary_kpi_month"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <!-- Record Rule for dl.salary.kpi.line -->
        <record id="dl_salary_kpi_line_multi_company_rule" model="ir.rule">
            <field name="name">Chi tiết bảng lương: đa công ty</field>
            <field name="model_id" ref="model_dl_salary_kpi_line"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <!-- Record Rule for dl.salary.kpi.bonus.year -->
        <record id="dl_salary_kpi_bonus_year_multi_company_rule" model="ir.rule">
            <field name="name">Cấu hình thưởng năm: đa công ty</field>
            <field name="model_id" ref="model_dl_salary_kpi_bonus_year"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>

        <!-- Record Rule for dl.tax.department -->
        <record id="dl_tax_department_multi_company_rule" model="ir.rule">
            <field name="name">Phòng ban thuế: đa công ty</field>
            <field name="model_id" ref="model_dl_tax_department"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        </record>
    </data>
</odoo>
